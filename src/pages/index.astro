---
import { Image } from "astro:assets";
import Layout from "../layouts/Layout.astro";
import TournamentCard from "../components/tournaments/TournamentCard.astro";
import { getUpcomingTournaments } from "../data/tournaments";

export const prerender = false;

const upcomingTournaments = await getUpcomingTournaments();

import teamCouch from "../assets/gallery/team-couch.jpg";
import teamTower from "../assets/gallery/team-tower.jpg";
import gamesStack from "../assets/gallery/games-stack.jpg";
import cardGame from "../assets/gallery/card-game.jpg";
import teamSofa from "../assets/gallery/team-sofa.jpg";

const games = [
  { id: "lorcana", name: "Lorcana", logo: "/images/games/lorcana.png", href: "/lorcana" },
  { id: "pokemon", name: "Pokémon TCG", logo: "/images/games/pokemon-tcg.webp", href: "/pokemon-tcg", comingSoon: true },
  { id: "riftbound", name: "Riftbound", logo: "/images/games/riftbound.png", href: "/riftbound", comingSoon: true },
];

const usps = [
  {
    title: "Přátelská atmosféra",
    text: "Herna, kde je každý vítán – bez zbytečné soutěživosti mimo stůl.",
  },
  {
    title: "Lepší ceny",
    text: "Karty, boostery a účast na turnajích za ceny, které dávají smysl.",
  },
  {
    title: "Turnaje a liga",
    text: "Pravidelná liga, jednorázové turnaje i prerelease nových sad.",
  },
  {
    title: "Komunita hráčů",
    text: "Spojujeme hráče Lorcany, Pokémonu i Riftboundu v Ostravě.",
  },
];

const faqPreview = [
  {
    q: "Jak zarezervuji místo na turnaji?",
    a: "Na stránce Turnaje vyberte termín a odešlete rezervaci – stačí jméno, e-mail a přezdívka, přihlášení uživatelé mají údaje předvyplněné.",
  },
  {
    q: "Musím platit předem?",
    a: "Ne, účastnický poplatek se platí až na místě před začátkem turnaje.",
  },
  {
    q: "Jaký je rozdíl mezi ligou a turnajem?",
    a: "Liga se hraje pravidelně (každý týden), turnaje jsou jednorázové akce a prerelease nových sad.",
  },
];
---

<Layout title="Herna pro hráče, od hráčů pro hráče">
  <!-- HERO -->
  <section class="relative overflow-hidden">
    <div class="pointer-events-none absolute -top-32 right-0 h-[32rem] w-[32rem] rounded-full bg-kostka-purple/20 blur-[120px]"></div>

    <div class="relative mx-auto max-w-6xl px-4 pb-16 pt-16 md:pb-24 md:pt-20">
      <div class="flex items-center gap-3">
        <span class="h-[3px] w-14 flex-none rounded-full bg-kostka-purple"></span>
        <p class="text-xs font-semibold uppercase tracking-[0.2em] text-kostka-purple">
          Herna pro hráče TCG · Ostrava-Zábřeh
        </p>
      </div>

      <div class="mt-5 max-w-4xl">
        <h1 class="text-4xl font-black leading-[1.1] tracking-tight text-white md:text-5xl lg:text-6xl">
          Vaše místo pro Lorcanu,<br class="hidden sm:block" />
          <span class="text-kostka-purple">Pokémon a Riftbound.</span>
        </h1>
        <p class="mt-6 max-w-2xl text-base text-white/70 sm:text-lg">
          Kostka TCG je herna, kterou jsme si postavili tak, jak bychom si ji sami přáli
          najít – s poctivými cenami, lidmi, co tomu rozumí, a partou, ke které se budete
          rádi vracet.
        </p>
      </div>

      <div class="mt-8 flex flex-wrap gap-4">
        <a
          href="/turnaje"
          class="rounded-full bg-kostka-purple px-8 py-3 text-sm font-semibold uppercase tracking-wide text-kostka-bg hover:opacity-90"
        >
          Zarezervovat termín turnaje
        </a>
        <a
          href="/kontakt#o-nas"
          class="rounded-full border border-white/20 px-8 py-3 text-sm font-semibold uppercase tracking-wide text-white hover:border-kostka-purple hover:text-kostka-purple"
        >
          Kdo jsme
        </a>
      </div>

      <div class="mt-12">
        <div class="flex flex-col gap-2">
          <p class="text-xs font-semibold uppercase tracking-[0.24em] text-white/55">Vyber si svou hru</p>
        </div>
        <div class="mt-4 grid gap-4 md:grid-cols-3">
          {
            games.map((game) => (
              <a
                href={game.href}
                aria-label={game.name}
                class={`game-card game-card--${game.id} group relative flex h-40 items-center justify-center overflow-hidden rounded-2xl border border-white/10 bg-kostka-surface/50 p-6 transition-colors duration-300 sm:h-44`}
              >
                <span class="game-card__glow pointer-events-none absolute inset-0 opacity-0 transition-opacity duration-500 group-hover:opacity-100" />

                {game.comingSoon && (
                  <span class="absolute right-3 top-3 z-10 rounded-full border border-kostka-purple/40 bg-kostka-purple/10 px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[0.16em] text-kostka-purple">
                    Připravujeme
                  </span>
                )}

                <img
                  src={game.logo}
                  alt={game.name}
                  class={`game-card__logo relative z-10 max-h-20 w-auto object-contain sm:max-h-24 ${game.comingSoon ? "opacity-60" : ""}`}
                />
              </a>
            ))
          }
        </div>
      </div>
    </div>

    <style>
      .game-card {
        isolation: isolate;
      }

      /* Jemná záře kolem celé karty (tlačítka) při hoveru */
      .game-card__glow {
        background: radial-gradient(circle at 50% 45%, var(--glow-color, rgba(157, 127, 224, 0.22)), transparent 65%);
      }

      /* Jemná záře uvnitř kolem celého loga při hoveru – kopíruje tvar loga,
         ne jen kruh uprostřed; stejná velikost/intenzita pro všechny hry */
      .game-card__logo {
        transition: filter 0.4s ease;
        filter: drop-shadow(0 0 0 transparent);
      }
      .game-card:hover .game-card__logo {
        filter:
          drop-shadow(0 0 3px var(--glow-color, rgba(157, 127, 224, 0.3)))
          drop-shadow(0 0 10px var(--glow-color, rgba(157, 127, 224, 0.3)));
      }

      .game-card:hover {
        border-color: var(--border-color, rgba(157, 127, 224, 0.4));
        box-shadow: 0 0 24px var(--glow-color, rgba(157, 127, 224, 0.18));
      }

      /* Lorcana – fialová */
      .game-card--lorcana {
        --glow-color: rgba(167, 139, 250, 0.3);
        --border-color: rgba(167, 139, 250, 0.5);
      }

      /* Pokémon TCG – žlutá */
      .game-card--pokemon {
        --glow-color: rgba(255, 209, 0, 0.3);
        --border-color: rgba(255, 209, 0, 0.5);
      }

      /* Riftbound – oranžová */
      .game-card--riftbound {
        --glow-color: rgba(255, 140, 40, 0.3);
        --border-color: rgba(255, 140, 40, 0.5);
      }

      /* Ve světlém motivu má karta téměř bílé pozadí – pastelové barvy z tmavého
         motivu by na něm skoro zmizely, proto tam záře používá sytější/tmavší
         odstíny, aby byla stejně kontrastní jako ve tmavém. */
      :global(html[data-theme="light"]) .game-card--lorcana {
        --glow-color: rgba(124, 58, 237, 0.35);
        --border-color: rgba(124, 58, 237, 0.55);
      }

      :global(html[data-theme="light"]) .game-card--pokemon {
        --glow-color: rgba(217, 119, 6, 0.35);
        --border-color: rgba(217, 119, 6, 0.55);
      }

      :global(html[data-theme="light"]) .game-card--riftbound {
        --glow-color: rgba(234, 88, 12, 0.35);
        --border-color: rgba(234, 88, 12, 0.55);
      }
    </style>

    <!-- USP strip -->
    <div class="relative mx-auto max-w-6xl px-4 pb-16 md:pb-24">
      <div class="grid gap-4 rounded-2xl border border-white/10 bg-kostka-surface/60 p-4 sm:grid-cols-2 lg:grid-cols-4">
        {
          usps.map((item) => (
            <div class="rounded-2xl border border-white/6 bg-white/[0.02] p-5 transition duration-300 hover:border-kostka-purple/50 hover:bg-kostka-purple/[0.04]">
              <div class="h-px w-10 rounded-full bg-kostka-purple"></div>
              <h3 class="mt-4 text-lg font-bold text-white">{item.title}</h3>
              <p class="mt-2 text-sm text-white/55">{item.text}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- NADCHÁZEJÍCÍ TURNAJE -->
  <section class="mx-auto max-w-6xl px-4 py-8">
    <div class="flex flex-wrap items-end justify-between gap-4">
      <div>
        <p class="text-xs font-semibold uppercase tracking-wide text-kostka-purple">Nadcházející turnaje</p>
        <h2 class="mt-2 text-2xl font-bold text-white md:text-3xl">Co nás čeká?</h2>
        <p class="mt-2 max-w-2xl text-sm text-white/55 md:text-base">
          Pravidelné ligy, jednorázové turnaje a prerelease nových sad. Všechny termíny najdeš v jednom přehledu.
        </p>
      </div>
      <a
        href="/turnaje"
        class="rounded-full border border-white/15 px-5 py-2 text-sm font-semibold text-white/80 hover:border-kostka-purple hover:text-kostka-purple"
      >
        Všechny turnaje →
      </a>
    </div>
    <div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {upcomingTournaments.length
        ? upcomingTournaments.slice(0, 3).map((event) => <TournamentCard event={event} />)
        : <p class="text-sm text-white/50">Zatím tu nejsou žádné naplánované termíny.</p>}
    </div>
    <p class="mt-8 flex items-center justify-center gap-2 text-center text-sm text-white/40">
      <img src="/images/icons/calendar.svg" alt="" class="h-4 w-4 flex-none" />
      Pravidelně přidáváme nové turnaje a ligy. Sledujte nás a buďte u toho!
    </p>
  </section>

  <!-- GALERIE -->
  <section class="bg-kostka-surface/40 py-16 md:py-24">
    <div class="mx-auto max-w-6xl px-4">
      <p class="text-xs font-semibold uppercase tracking-wide text-kostka-purple">Komunita</p>
      <div class="mt-2">
        <h2 class="text-2xl font-bold text-white md:text-3xl">Ze hry i z turnajů</h2>
      </div>
      <div class="mt-8 grid grid-cols-2 gap-3 md:grid-cols-4">
        <Image src={cardGame} alt="Hráčka u karetní hry v KOSTKA TCG" class="col-span-2 row-span-2 h-full w-full rounded-2xl object-cover" widths={[400, 800]} sizes="(min-width: 768px) 400px, 50vw" />
        <Image src={teamTower} alt="Tým KOSTKA TCG s věží krabic her" class="h-full w-full rounded-2xl object-cover" widths={[300, 600]} sizes="300px" />
        <Image src={gamesStack} alt="Naskládané krabice her v herně" class="h-full w-full rounded-2xl object-cover" widths={[300, 600]} sizes="300px" />
        <Image src={teamCouch} alt="Tým KOSTKA TCG na gauči s hrami" class="h-full w-full rounded-2xl object-cover" widths={[300, 600]} sizes="300px" />
        <Image src={teamSofa} alt="Tým KOSTKA TCG při turnaji" class="h-full w-full rounded-2xl object-cover" widths={[300, 600]} sizes="300px" />
      </div>
    </div>
  </section>

  <!-- FAQ TEASER -->
  <section class="bg-kostka-surface/40 py-16 md:py-24">
    <div class="mx-auto max-w-3xl px-4">
      <h2 class="text-2xl font-bold text-white md:text-3xl">Časté dotazy</h2>
      <div class="mt-8 divide-y divide-white/10">
        {
          faqPreview.map((item) => (
            <details class="group py-4">
              <summary class="cursor-pointer list-none font-semibold text-white marker:content-none">
                {item.q}
              </summary>
              <p class="mt-2 text-white/60">{item.a}</p>
            </details>
          ))
        }
      </div>
      <a href="/faq" class="mt-6 inline-block text-sm font-semibold text-kostka-purple hover:underline">
        Všechny otázky →
      </a>
    </div>
  </section>
</Layout>
